Для NEXT:
У вершины *здесь значение SECOND от ребра* есть продолжение, поэтому ИДЁМ ДАЛЬШЕ.

Для NO_BRIDGE:
Мы вернулись к вершине *здесь значение SECOND от ребра*, значит в неё можно вернуться не одним путём.
Таким обрзом рёбра на нашем пути (*здесь пары значений рёбер(ФОРМАТ: FIRST-SECOND) через запятую
из списка НЕ МОСТЫ*) НЕ ЯВЛЯЮТСЯ МОСТАМИ.

Для IS_BRIDGE:
В вершину *здесь значение FIRST от ребра* ведёт путь только чреез вершину *здесь значение SECOND от ребра*.

Для END:
Весь граф пройден!
